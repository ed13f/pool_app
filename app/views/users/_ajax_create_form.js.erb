$( document ).ready(function() {
	$('.users.card-list .list').prepend("<%= escape_javascript(render :partial => 'users/user-card-individual', :locals => { :user => user })  %>")
	$('#user-form').removeClass('active');
});