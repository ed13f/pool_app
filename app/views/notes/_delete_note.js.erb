$( document ).ready(function() {
  $(".note-<%= @note.id%>").remove();
  if ( $.trim($(".customer-notes-list ul").text()).length == 0){
    $(".customer-notes-list ul").prepend("<li class='no-notes'>No Notes</li>");
  }
});