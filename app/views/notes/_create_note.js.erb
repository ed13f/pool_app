$( document ).ready(function() {
  $(".no-notes").remove();
  $(".description-input").val("");
  $(".notes-widget ul").prepend("<%= j render partial: 'note_item', locals: {note: @note} %>");
  $(".description-input").attr("placeholder", "Description*");
  $(".description-input").removeClass("error");
});